[package]
name = "zerocopyvision"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
# flutter_rust_bridge glue is added by the codegen; pin it when you wire it up.
# flutter_rust_bridge = "2"
thiserror = "1"

[dev-dependencies]
