name: zerocopyvision
description: Real-time on-device vision with a zero-copy Rust core.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0
  # flutter_rust_bridge: ^2.0.0   # uncomment after running the codegen

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
